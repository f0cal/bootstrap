# {% for repo in project.repos %}
# {% set cmd = "git -C %s/%s rev-parse --verify --short HEAD" | join(code_dir, repo.name) %}
# {% set rev = salt['cmd.run'](cmd) %}
# {% do repo.update({'rev': rev}) %}
# {% endfor %}

{{ project | to_yaml() }}
